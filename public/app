<!DOCTYPE HTML>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Main</title>
        <style>
            html,head,body { padding:0; margin:0; }

            body { font-family: calibri, helvetica, arial, sans-serif; }
        </style>

        <div id="event-creator", style="display: none;">
            <div id="email">event.creator@gmail.com</div>
            <div id="current-user-id">30</div>
        </div>

        <link rel="stylesheet" href="nb_integration.css" />
        <script src="nb_integration.js"></script>
    </head>
    <body>
        <script type="text/javascript">
            var email = document.getElementById("email").textContent;
            var access_token = "DEFAULT";
            var rootURL = "http://localhost:3000";
            var slug = "skarger";
            var currentUserID = parseInt(document.getElementById("current-user-id").textContent, 10);

            Elm.Main.fullscreen({
                email: email,
                authorID : currentUserID,
                access_token: access_token,
                rootURL: rootURL,
                slug: slug
            })
        </script>
    </body>
</html>
