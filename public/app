<!DOCTYPE HTML>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Main</title>
        <style>
            html,head,body { padding:0; margin:0; }

            body { font-family: calibri, helvetica, arial, sans-serif; }
        </style>

        <div id="event-creator", style="display: none;">
            <div id="email">event.creator@gmail.com</div>
            <div id="current-user-id">30</div>
        </div>
    </head>

    <body>
        <div id="events-interface"></div>
        <script>
            function renderEvents() {
                var node = document.getElementById("events-interface");
                var email = document.getElementById("email").textContent;
                var currentUserID = parseInt(document.getElementById("current-user-id").textContent, 10);

                NBIntegration.eventInterface(node, email, currentUserID);
            }
        </script>

        <script defer src="http://localhost:3000/embed/js?slug=skarger&callback=renderEvents"></script>
    </body>
</html>
