{
  "name": "nb_integration",
  "version": "1.0.0",
  "description": "NationBuilder integration client",
  "main": "index.js",
  "repository": "git@github.com:climatecrew/nb_integration.git",
  "author": "Stephen Karger <stephen.karger@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": "9.10.1"
  },
  "dependencies": {
    "elm": "^0.18.0"
  },
  "scripts": {
    "test": "elm-test",
    "postinstall": "yarn run elm-package install -y && yarn run elm-make assets/Main.elm --output public/nb_integration.js",
    "heroku-postbuild": "gzip -f public/nb_integration.js"
  },
  "devDependencies": {
    "elm-test": "^0.18.12",
    "markdown-toc": "^1.2.0"
  }
}
